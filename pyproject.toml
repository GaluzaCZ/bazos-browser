[project]
name = "bazos-sniper"
version = "1.0.0"
description = "Modular Bazos marketplace browser"
requires-python = ">=3.12"
dependencies = ["requests>=2.32", "beautifulsoup4>=4.12", "lxml>=5.2"]

[project.scripts]
bazos-sniper = "main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
